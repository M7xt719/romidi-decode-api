Flask>=3.1,<4
gunicorn>=26,<27
requests>=2.32,<3
numpy>=1.26,<3
py7zr>=0.21,<1      # .7z archives (pure-Python, no system binary)
rarfile>=4.2,<5     # .rar archives (needs the 'unar' binary — installed in the Dockerfile)
